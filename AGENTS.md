# Unity Agent Rules 维护规则

## 范围与所有权

- 本仓库只维护跨 Unity 项目共享的 Agent 细则；消费项目通过 Git submodule 固定本仓库 commit，并在各自根 `AGENTS.md` 中提供任务路由。
- 项目路径、业务资源、Wrapper/Prefab、项目专属工作流、产品行为和验证权限必须留在消费项目，不得为了复用方便写进共享规则。
- 共享规则是唯一正文权威。禁止在消费项目复制一份可编辑正文、依赖机器专属绝对路径、符号链接、Unity PackageCache 或运行时网络下载。

## 修改与发布

- 修改共享契约前先审计全部消费项目的根规则、专项规则和冲突边界；共享规则不得扩大消费项目的测试、运行时、视觉、破坏性操作或无关发布权限。用户明确要求新增、纠正、优化、删除或沉淀共享契约时，该请求本身已经授权本仓库的提交、语义化版本 tag、推送，以及全部消费项目仅包含规则 overlay/gitlink 的提交与推送；这是规则迁移的窄范围专项授权，不得扩展到消费项目其他脏改、产品发布或 Unity 验证。
- 同一逻辑变更必须同步维护对应 instruction、README 和 CHANGELOG；删除过时或重复规则，不能保留新旧双轨。
- 使用语义化版本 tag。发布事务固定为：确认远端基线并审查 diff；提交共享规则；创建并推送 tag 与权威分支；把所有可发现消费项目的 submodule 固定到同一 commit；同步必要本地 overlay；分别提交并推送各消费项目当前分支。仅留下 dirty/detached submodule、只推共享仓库或只更新一个消费项目都不算迁移完成，也不得以“发布权限独立”为由停下。
- 文档变更只做静态验证：检查引用、LF/EOF、`git diff --check` 和消费项目 gitlink/路由；不得因此启动 Unity、编译或测试。

## Git

- 默认在当前分支工作。除非用户明确要求，不创建额外分支或 PR。
- 发布前确认暂存区只包含本次共享规则改动；工作树可存在用户无关改动，但必须保持未暂存且不得进入提交。提交并推送权威仓库后再更新消费项目 gitlink；若共享 submodule 处于 detached HEAD，先把当前远端权威分支安全附着到同一基线再提交，不能把未提交的 detached 工作树当成交付结果。
